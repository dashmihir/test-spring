
spring:
  datasource:
    url: jdbc:postgresql://dashmihir@localhost:5432
    username: postgres
    password: ****
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect